# Article III checkpoint — 2026-09-13 — Affine flux defect lift

## Status

`PROVED_CENTERED_BLIND_SECTOR / AFFINE_SHIFT_DEFECT_CLOSED / MAIN TREE-CAP GAP STILL OPEN`

## New theorem

See:

`ARTICLE-III-AFFINE-FLUX-DEFECT-LIFT-v0.1.md`

The centered metaplectic/Weyl architecture has an explicit real blind sector

\[
\mathfrak A_p^-
=\{D_\phi:\phi:\mathbb F_p\to\mathbb R\text{ odd}\},
\qquad
\dim\mathfrak A_p^-=(p-1)/2,
\]

with

\[
D_\phi(W_h)=\sum_k\phi([k,h])W_k.
\]

Every centered metaplectic/Weyl engineered Coxeter square annihilates this entire sector.

Inside it, the unique Hamiltonian line is generated by parity

\[
\Pi=F^2,
\]

with coefficient function

\[
\phi_\Pi(t)\propto\operatorname{Im}(\omega^{t/2}).
\]

Hence centered Clifford designs necessarily have exactly the explicit symmetry-forced non-Hamiltonian blind subspace dimension

\[
\boxed{(p-3)/2}.
\]

This explains the observed centered extra nullities `1` for `p=5` and `2` for `p=7`.

## Affine lift

For the affine quadratic chirp

\[
P_\tau=\operatorname{diag}(\omega^{(j^2+\tau j)/2}),
\qquad \tau\ne0,
\]

and any face `(P_tau,W_g)` with `g_1!=0`, the restriction to the blind sector has kernel exactly the parity Hamiltonian line:

\[
\boxed{
\ker(D_\phi\mapsto K_{D_\phi}(P_\tau,W_g))
=\mathbb R\delta_\Pi.
}
\]

The proof uses the exact coefficient factorization

\[
\widehat K(x,t)
=a_x+\omega^t\overline{a_{-x}}
+(\omega^{-\tau x/2}-1)\psi(t),
\qquad
\psi(t)=\phi(t)\omega^{t/2}.
\]

For `tau!=0` and `x!=0`, vanishing forces

\[
\psi(t)\in\operatorname{span}\{1,\omega^t\},
\]

and odd reality leaves only

\[
\phi(t)\propto\operatorname{Im}(\omega^{t/2}),
\]

i.e. the parity Hamiltonian mode.

## Quantitative gap

On the orthogonal complement of the parity line,

\[
\psi\perp\operatorname{span}\{1,\omega^t\}.
\]

Summing the projected coefficient over all `x!=0` gives

\[
\|K_{D_\phi}(P_\tau,W_g)\|_{2,p}^2
\ge2p\|\phi\|_2^2,
\]

while

\[
\|D_\phi\|_{sop}^2
=\frac{p^2-1}{p}\|\phi\|_2^2.
\]

Therefore

\[
\boxed{
\|K_{D_\phi}(P_\tau,W_g)\|_{2,p}
\ge
\sqrt{\frac{2p^2}{p^2-1}}\,
\|D_\phi\|_{sop}.
}
\]

So the affine symmetry-breaking step has a **uniform constant gap**, not merely an inverse-polynomial one.

## Research consequence

The affine-flux mystery is closed. The remaining odd-prime sharp theorem is now strictly the geometric tree/cap problem outside `A_p^-`:

1. prove the complete `Q_tau` tree block has full row rank with inverse-polynomial gap;
2. prove the final Fourier cap has rank `p^2+1` on the `2p^2` residual kernel and leaves exactly Hamiltonian gauge.

The next attack should target a twisted-incidence factorization of the full `Q_tau` tree block, now with the symmetry-forced defect removed from consideration.
