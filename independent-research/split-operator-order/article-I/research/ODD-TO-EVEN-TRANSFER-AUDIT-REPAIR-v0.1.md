# Odd-to-even extension-ready transfer — centered scalar-one audit repair

**Article I post-publication research note — v0.1**  
**Author:** Malachevsky, A.A.  
**ORCID:** 0009-0008-6009-3196  
**Status:** `PROVED_TRANSFER_REPAIRED_IN_SL / CENTERED_FORMULAS_CERTIFIED / UNITARY_RETURN_CERTIFIED`

## 1. Purpose

`ODD-TO-EVEN-EXTENSION-READY-v0.1.md` proved the intended parity transfer

`ER_d -> ER_(d+1)`

for odd `d>=3`, but the publication audit identified three proof-compression defects:

1. the carrier was written in centered language while several symbols were left in an uncentered convention;
2. the two-tail local and binding reductions were imported without a typed derivation;
3. determinant normalization was invoked as though scalar rescaling were harmless after scalar-one embedding.

The third point is a genuine invalid proof step: if a dimension-`n` transport `A` is replaced by `cA` and then embedded as `diag(cA,1)` in dimension `n+1`, this is **not** a scalar multiple of `diag(A,1)`. Hence the centered scalar-one measurement is not generally homogeneous under that normalization.

This note repairs the theorem without using post hoc scalar normalization. Every complex witness is constructed directly in `SL_n(C)`, after which Zariski density of `SU(n)` in `SL_n(C)` gives genuine unitary witnesses.

The result is a proof repair, not a new theorem family.

Throughout

`d>=3` is odd,

`n=d+1`,

`N=d+2`.

Inside `M_N(C)` put

`C_d=M_d(C) direct-sum C I_2`,

`B_n=M_n(C) direct-sum C`.

Assume the old dimension-`d` design is extension-ready.

## 2. Exact centered scalar-one coordinate

A normalized relative map on `B_n` is represented by a complex-linear map

`F:M_n(C)->M_N(C)`

through

`D(diag(X,a))=F(X-a I_n)`.

For dimension-`n` transports `A,B`, embedded as

`A~=diag(A,1)`, `B~=diag(B,1)`

in `M_N`, define

`A0=A-I_n`, `B0=B-I_n`.

The exact centered branch operator is

`K_F(A,B)`

`=F(AB-I_n)-F(A0) B~ - A~ F(B0)`.

Since

`AB-I=A0+B0+A0 B0`,

this may equivalently be written as the bilinear centered defect

`K_F(A,B)=F(A0 B0)-F(A0)B0-A0 F(B0)`

when the left/right module actions are restricted to the same output block. The first displayed formula is the master formula used below.

For a scalar output functional `phi`, the centered scalar defect is simply

`delta_phi(A,B)=phi((A-I)(B-I))`.

The paired branch is obtained from

`(A,B) -> (B^(-1),A^(-1))`.

## 3. Relative coordinates

Modulo the old algebra `C_d`, put

`x_j=E_(j,n)`, `y_j=E_(n,j)`, `j=1,...,d`,

and

`e=E_(n,n)`.

Write

`P_j=F(x_j)`, `Q_j=F(y_j)`, `R=F(e)`.

The relative Hamiltonian gauge has dimension `3`, generated by the lower `2 x 2` traceless directions

`E_(n,n)-E_(N,N)`, `E_(n,N)`, `E_(N,n)`.

The dimension count from the original transfer note remains unchanged:

`dim relative quotient=(2d+1)N^2-3`.

The doubly embedded old odd ER design is short of the `C_d` restriction quotient by exactly five coordinates: one regular direction and four copies of one scalar direction.

## 4. Determinant-one carrier parameter space

Do not normalize a `GL_n` carrier after the calculation. Parameterize the block Levi subgroup of `SL_n` directly:

`A_C=diag(C,u)`, `u=(det C)^(-1)`,

`B_D=diag(D,v)`, `v=(det D)^(-1)`,

with `C,D in GL_d(C)`.

Thus `A_C,B_D in SL_n(C)` identically.

Their scalar-one embeddings are

`A~=diag(C,u,1)`,

`B~=diag(D,v,1)`.

On the pure relative variable `R`, with all `P_j,Q_j=0`, the exact master formula gives

`T_+(R)=(uv-1)R-(u-1)R B~-(v-1)A~R`,

and for the inverse branch

`T_-(R)=(u^(-1)v^(-1)-1)R`

` -(v^(-1)-1)R A~^(-1)`

` -(u^(-1)-1)B~^(-1)R`.

These are the carrier formulas from the original note, now derived in the correct centered scalar-one coordinate.

Both annihilate

`E_(n,N)`, `E_(N,n)`.

## 5. The determinant-one carrier has generic `R`-kernel exactly equal to gauge

Restrict temporarily to diagonal

`C=diag(c_1,...,c_d)`, `D=diag(d_1,...,d_d)`.

Then

`u=(prod_i c_i)^(-1)`, `v=(prod_i d_i)^(-1)`.

For an output matrix unit `E_(r,s)`, the forward carrier coefficient is

`eta^+_(r,s)=uv-1-(u-1)b_s-(v-1)a_r`,

where

`a=(c_1,...,c_d,u,1)`,

`b=(d_1,...,d_d,v,1)`.

For `(r,s)=(n,N)` and `(N,n)` this Laurent polynomial is identically zero.

For every other position it is not the zero Laurent polynomial. For example:

- `(n,j)`: `(u-1)(1-d_j)`;
- `(i,n)`: `(v-1)(1-c_i)`;
- `(N,j)`: `(u-1)(v-d_j)`;
- `(i,N)`: `(v-1)(u-c_i)`;
- `(n,n)`: `-(u-1)(v-1)`;
- `(N,N)`: `(u-1)(v-1)`;

and the old-old coefficient is plainly nonconstant in the independent torus variables.

Hence the finite intersection

`eta^+_(r,s)!=0`

for all nongauge positions is a nonempty Zariski-open subset of the diagonal torus. At such a point the forward operator alone has kernel exactly

`span{E_(n,N),E_(N,n)}`.

Therefore the full two-branch carrier has `R`-rank

`N^2-2`

on a nonempty Zariski-open subset of the determinant-one Levi parameter space `GL_d^2`.

## 6. Old regular defect survives modulo the determinant-one carrier

Let `F_reg` represent the one missing regular old class. In centered old coordinates define the bilinear Leibniz defect

`B_F(X,Y)=F_reg(XY)-F_reg(X)Y-XF_reg(Y)`.

Because the old class is nonzero modulo derivations,

`B_F` is not identically zero.

Choose `D in SL_d(C)` such that

`I-D` is invertible

and choose `C_0 in GL_d(C)` such that

`H_0:=B_F(C_0,D-I) !=0`.

Such choices exist: if `B_F(X,D-I)` vanished for every `X` and every `D in SL_d`, the span of `{D-I:D in SL_d}` would force `B_F=0`, hence `F_reg` would be a derivation.

Now set

`C=c C_0`, `c in C^*`,

so

`u=(det C)^(-1)=c^(-d)(det C_0)^(-1)`,

while `v=1` because `D in SL_d`.

Let

`h_+=B_F(C-I,D-I)`,

`h_-=B_F(D^(-1)-I,C^(-1)-I)`.

Then as `|c|->infinity`,

`h_+=c H_0+O(1)`,

while `h_-=O(1)`.

At `v=1`, the relative `R`-image on the old `d x d` output block satisfies

`g_+=(u-1)R(I-D)`,

`g_-=(u^(-1)-1)(I-D^(-1))R`.

Thus an old pair `(h_+,h_-)` belongs to the relative image only if

`h_-=u^(-1) J_D(h_+)`,

where

`J_D(X)=D^(-1)(I-D)X(I-D)^(-1)`

is invertible.

The right-hand side has leading size/order

`c^(d+1) (det C_0) J_D(H_0)`,

whereas the left-hand side remains bounded. Therefore the equality fails for all sufficiently large `c` outside a finite exceptional set.

So regular old-defect survival is a nonempty open condition **inside the determinant-one Levi carrier family**.

Because this condition is open, it persists after a small generic perturbation of `D` away from determinant one, hence after `v` moves away from `1` to the generic `R`-rank locus.

## 7. All four old scalar directions survive modulo the determinant-one carrier

Let `phi` represent the missing scalar class. It is nonzero.

Choose invertible `C_0,D_0` such that

`phi(C_0 D_0)!=0`.

Put

`C=c C_0`, `D=e D_0`,

so

`u=c^(-d)(det C_0)^(-1)`,

`v=e^(-d)(det D_0)^(-1)`.

Write

`x=C-I`, `y=D-I`.

For the tail matrix unit `K=E_(a,b)`, `a,b in {n,N}`, the forward scalar coefficient is

`s^+_(ab)=phi(CD-I)-phi(C-I)b_b-phi(D-I)a_a`.

At `(N,N)`, this reduces exactly to

`s^+_(NN)=phi(xy)`.

Hence

`s^+_(NN)=ce phi(C_0D_0)+O(c+e+1)`.

The inverse coefficient `s^-_(NN)` stays bounded as `|c|,|e|->infinity`, whereas

`1/(uv)`

has order `c^d e^d`.

Therefore

`s^+_(NN)/(uv)-s^-_(NN)`

has nonzero leading term of order

`c^(d+1)e^(d+1)`.

Thus the `NN` scalar direction is independent of the diagonal `R` image for generic large `c,e`.

At `(n,n)`,

`s^+_(nn)`

has the same leading term `ce phi(C_0D_0)`, while `s^-_(nn)` can grow at most like `c^d+e^d` because only the factors `u^(-1),v^(-1)` multiply bounded inverse-centered scalar values. Hence

`s^-_(nn)-s^+_(nn)/(uv)`

is likewise nonzero for generic large `c,e`.

The two off-diagonal scalar directions `E_(n,N)` and `E_(N,n)` are automatically independent of the relative `R` image because the latter vanishes at exactly those two gauge positions.

Therefore all four scalar old directions survive modulo the `R` image on a nonempty open subset of the determinant-one carrier family.

## 8. Determinant-one five-defect carrier theorem

The carrier `R`-rank condition, regular-old survival, and four-scalar survival are nonempty Zariski-open conditions on the irreducible parameter space

`GL_d(C)^2 ~= {block-Levi pairs in SL_n(C)^2}`.

Their finite intersection is nonempty.

### Theorem 8.1 — corrected carrier

There exist

`A,B in SL_n(C)`

of block-Levi form such that the scalar-one embedded carrier measurement on

`E_5 direct-sum M_N(C)_R`

has kernel exactly

`span{E_(n,N),E_(N,n)}`.

Equivalently its rank is

`(N^2-2)+5=N^2+3`.

No determinant rescaling is used.

## 9. Determinant-one local reduction with a two-dimensional tail

Choose

`Lambda=diag(lambda_1,...,lambda_d,1) in SL_n(C)`

with

`prod_(i=1)^d lambda_i=1`.

Choose the old eigenvalues on the nonempty Zariski-open torus locus satisfying

- `lambda_i!=0,1`;
- apart from the duplicated tail eigenvalues `lambda_n=lambda_N=1`, the equation

  `lambda_r lambda_s=lambda_j`

  has only the intended solutions

  `(r,s)=(j,n),(j,N),(n,j),(N,j)`.

For each `j<=d`, let `U_j in SL_n(C)` be identity outside `span{e_j,e_n}` and equal to

`[[1,1],[1,2]]`

on that plane.

After the carrier has reduced

`R=r E_(n,N)+s E_(N,n)`, 

an unperturbed local face `(U_j,Lambda)` has exact relative branch equations, entrywise,

`K^+_(r,s)`

`=(1-lambda_s)(p_(r,s)+R_(r,s))`

` +(lambda_j-lambda_s) q_(r,s)`,

and

`K^-_(r,s)`

`=(lambda_r^(-1)-lambda_j^(-1)) p_(r,s)`

` +(lambda_r^(-1)-1) q_(r,s)`.

Here the full output eigenvalue list is

`(lambda_1,...,lambda_d,1,1)`.

Away from the carrier gauge positions the `R` term is zero. At the carrier gauge positions its coefficient `1-lambda_s` is also zero. Thus `R` does not disturb the local `P_j,Q_j` reduction.

The `2 x 2` determinant on `(p,q)` is

`- (lambda_j-1)(lambda_j-lambda_r lambda_s)/(lambda_j lambda_r)`.

By the chosen multiplicative-Sidon-tail condition it vanishes exactly at

`(j,n),(j,N),(n,j),(N,j)`.

At the first two positions `P_j` survives, and at the last two `Q_j` survives. Therefore the common local kernel is exactly

`P_j=alpha_j E_(j,n)+beta_j E_(j,N)`,

`Q_j=gamma_j E_(n,j)+delta_j E_(N,j)`.

This supplies the typed two-tail reduction omitted from the original note.

## 10. First binding scale with determinant exactly one

Fix for each `j` an old index

`ell_j!=j`.

Let `mu=1+epsilon` and define the determinant-one diagonal perturbation

`D_j(epsilon)`

by

- entry `mu` at coordinate `n`;
- entry `mu^(-1)` at coordinate `ell_j`;
- entry `1` elsewhere.

Then

`det D_j(epsilon)=1`

and

`B_j(epsilon)=Lambda D_j(epsilon) in SL_n(C)`.

The compensating old coordinate `ell_j` is disjoint from the exceptional output positions used below. Direct substitution in the centered master formula gives **exactly**

`K^+_(j,N)=(mu-1)(beta_j-r)`,

and in the inverse branch

`K^-_(N,j)=(1-mu^(-1))(delta_j-s)`.

Thus for every sufficiently small `epsilon!=0`,

`beta_j=r`,

`delta_j=s`.

The other two local coordinates `alpha_j,gamma_j` remain free at this stage. Hence the residual scalar kernel has dimension `2d+2` with coordinates

`alpha_1,...,alpha_d, gamma_1,...,gamma_d, r,s`.

## 11. Second binding scale with determinant exactly one

Fix an edge `{j,k}`, `j!=k`, and choose an old compensating index

`ell_(jk) notin {j,k}`.

Such an index exists because `d>=3`.

Put

`K_(k,n)=E_(k,n)+E_(n,k)`

and define

`C_(jk)(t)`

as the diagonal matrix with entry

`(1-t^2)^(-1)`

at coordinate `ell_(jk)` and `1` elsewhere.

Then

`P_(jk)(t)=(I+t K_(k,n)) C_(jk)(t)`

satisfies

`det P_(jk)(t)=1`,

because

`det(I+tK_(k,n))=1-t^2`.

Moreover `C_(jk)(t)` is the identity on the three-dimensional subspace

`span{e_j,e_k,e_n}`.

Therefore, on the old-old exceptional output entries `(j,k)` and `(k,j)`, the local perturbation is **exactly the same three-coordinate calculation** as in `GLOBAL-BINDING-LEMMA-v0.1.md` with the new-coordinate eigenvalue set to `1`.

The second-order reduced Schur coefficients are therefore

`- lambda_k (lambda_j-1) / [lambda_j (lambda_k-1)]`

`  * (alpha_j+gamma_k)`

and

`(lambda_j-1)/(lambda_k-1)`

`  * (alpha_k+gamma_j)`.

Both prefactors are nonzero.

The tail variables `beta,delta,r,s` cannot enter these two old-old output entries because every one of them carries an `N` index and all new perturbations leave the external coordinate `N` invariant.

After the first binding scale has imposed

`beta_j=r`, `delta_j=s`,

the cross-plane perturbation therefore enforces

`alpha_j+gamma_k=0`,

`alpha_k+gamma_j=0`.

## 12. Graph binding and simultaneous two-scale rank lift

Choose the `d`-edge connected non-bipartite graph

`1-2-3-1`

plus

`1-j`, `j>=4`.

For each local face choose the corresponding cross-plane partner `k_j`.

Set

`a_j=alpha_j`, `b_j=-gamma_j`.

The edge equations become

`a_j=b_k`, `a_k=b_j`.

Connectivity plus the odd cycle gives

`alpha_1=...=alpha_d=q`,

`gamma_1=...=gamma_d=-q`.

Together with `r,s`, the remaining kernel is exactly three-dimensional, matching the relative Hamiltonian gauge.

To combine the two perturbations rigorously, regard the local-face family as analytic in two parameter collections `epsilon` and `t`. In adapted kernel/cokernel coordinates, the first reduced Schur block has a nonzero order-`epsilon` minor on the `2d` far-tail coordinates, while on its kernel the next reduced Schur block has a nonzero order-`t^2` minor of rank `2d-1` given by the graph operator above.

The iterated Schur-complement expansion therefore contains a maximal minor with nonzero leading monomial

`C epsilon^(2d) t^(2(2d-1))`, `C!=0`.

Equivalently one may first choose sufficiently small nonzero `epsilon`, where the first-scale rank is fixed, and then choose sufficiently small generic `t`; the graph minor varies analytically and remains nonzero near the displayed `epsilon=0` coefficient.

Hence one determinant-one choice of the same `d` local faces resolves the entire relative quotient modulo the three-dimensional gauge.

## 13. Corrected odd-to-even transfer theorem in `SL_n(C)`

The old odd ER design has

`(d^2-1)/2`

faces.

Add one determinant-one carrier and the `d` determinant-one local/binding faces above. The total is

`(d^2-1)/2+d+1=(d+1)^2/2=n^2/2`.

The carrier repairs the five old missing restriction coordinates and removes the `R` bulk. The other `d` faces resolve the remaining relative quotient modulo exactly the three relative derivations.

Therefore the embedded dimension-`n` design attains

`R(B_n)=(n^2-1)N^2+2`,

the structural extension-ready ceiling for even `n`.

Native full rank follows by restricting the fully resolved `B_n` quotient to normalized maps supported on the `M_n` block.

### Theorem 13.1 — repaired odd-to-even extension-ready transfer

For every odd `d>=3`,

`ER_d -> ER_(d+1)`.

The transfer uses exactly `d+1` new faces and saturates the information-theoretic face count.

## 14. Return to genuine unitary Coxeter faces

All complex witnesses above lie in `SL_n(C)` **before** any rank calculation. No scalar determinant normalization is used.

Fix the old odd ER design. A maximal embedded minor of the combined measurement operator is a regular/rational function of the new `SL_n(C)` transport pairs, with denominators nonzero on the chosen open chart. The preceding construction gives one complex point where this minor is nonzero.

`SU(n)` is Zariski dense in `SL_n(C)`. Therefore the same minor is nonzero for some tuple of unitary determinant-one transport pairs.

The engineered-square realization theorem from `ALL-D-COXETER-OD2-v0.1.md` realizes every ordered pair `U,V in SU(n)` as the first two contextual edge transports of an actual adjacent-transposition Coxeter square. Hence the repaired transfer theorem holds for genuine unitary Coxeter faces.

## 15. Audit disposition

The publication-audit findings are resolved as follows:

- **A2 centered carrier convention:** CLOSED by Sections 2, 4, 6, 7;
- **A3 two-tail local/binding reduction:** CLOSED by Sections 9–12;
- **A4 determinant normalization / unitary return:** the old scalar-rescaling justification is WITHDRAWN and replaced by the direct `SL_n` construction in Sections 4, 10, 11, 14.

Thus the odd-to-even transfer is certified at theorem level within the same finite-dimensional first-order model as the odd ER theorem.

Combined with the audited all-odd ER theorem, extension-ready minimal designs exist in every dimension `d>=3`. Consequently the research theorem

`L_d^Cox=floor(d^2/2)`

for every `d>=3`

survives the parity-transfer audit.

Publication release still requires manuscript consolidation, related-work/bibliography audit, theorem numbering, and final source/PDF checks; those are publication-hygiene tasks, not remaining existence-proof obligations.
