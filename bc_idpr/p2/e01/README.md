# BC-IDPR P2-E01

## Data-Derived One-Cycle Coefficient Transition Registry at eta=1

Status: `INCONCLUSIVE_GLUING__CENTER_REGISTRY_CONSTRUCTED`.

P2-E01 is the first data-derived transition-registry experiment after P2 Contract 00. It uses the five released P5 centre certificates on typed coefficient spaces `V_a ~= R^2` and the frozen one-cycle graph

`U1 -> U2 -> U3 -> U4 -> EQ -> U1`.

Each edge transition is obtained by minimizing the equal-weight joint congruence objective for trace-normalized operator and measurement Gram matrices.

Centre result:

- projective cycle angle: `0.001318320 rad` (`0.075534170 deg`);
- centre cycle defect `||H-I||_op = 1.318320e-3`;
- smallest twisted-incidence singular value: `2.636640e-4`;
- propagated conservative angular uncertainty: about `15.678894 deg`.

The observed centre defect is much smaller than the propagated uncertainty. Exact closure is neither certified nor excluded. P2-G2 therefore remains `INCONCLUSIVE`.

The next admissible object is `P2-E02 Oriented Cross-Overlap Edge Certification`. It must keep the graph frozen and provide a shared comparison carrier, oriented cross-Gram data, sign convention, singular-value lower bound, and certified edge defect for every edge.

Claim ceiling: finite eta=1 coefficient-level diagnostic only. No support-space identification, eta-uniform gluing, global positivity theorem, manifold theorem, or physical spacetime interpretation.

Upstream publication: BC-IDPR P5, DOI `10.5281/zenodo.21444321`.
