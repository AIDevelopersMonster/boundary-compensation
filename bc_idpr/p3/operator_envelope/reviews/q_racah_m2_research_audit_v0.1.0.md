# BC-IDPR-P3-B-02 — Direct q-Racah M2 Research Audit

**Status:** internal mathematical and computational review, v0.1.0  
**Date:** 2026-07-16  
**Scope:** three-channel direct q-Racah envelope; CERT and physical interpretation remain blocked.

## 1. Reviewed claim

The reviewed claim is relative and two-part:

1. an exact invariant proves operator nontriviality on the entire compact interval after quotienting independent affine normalization and simultaneous basis conjugation;
2. a preregistered 33-point high-precision grid supplies quantitative Hilbert--Schmidt and max-operator-norm lower bounds.

These two statements must not be conflated. The grid minimum is not claimed as a continuum minimum.

## 2. Methodological advance over M1

M2 materially strengthens M1:

- carrier dimension increases from two to three;
- the recoupling matrix is generated by a direct finite q-Racah sum rather than a closed two-channel formula;
- nuisance shifts and scales are independent for `X` and `Y`;
- all three `so(3)` conjugation tangents are included;
- a compact interval replaces a single-point calculation;
- an exact spectral invariant gives a continuum sign certificate;
- trace duality gives a separate operator-norm quotient lower bound on the grid.

## 3. Numerical findings

At `theta=pi/8`:

\[
m_{\mathrm{HS}}=2.5422533772484144629,
\qquad
\widehat m_{\mathrm{HS}}=0.16340718318126214185.
\]

On the 33-point interval grid:

\[
\min m_{\mathrm{HS}}=1.2292366584592391903,
\]

\[
\min\widehat m_{\mathrm{HS}}=0.07776396080237919195,
\]

and the minimum dual-certified max-operator-norm quotient distance is

\[
0.53229704247648650253.
\]

The nuisance Gram matrix remains safely nonsingular on the grid:

\[
\lambda_{\min}\ge0.7639320225002103,
\qquad
\kappa\le14.72334929919162.
\]

## 4. Continuum theorem inside the declared envelope

For the three eigenvalues of `X`, the affine spectral-shape invariant satisfies

\[
\mathcal R(x)=
\frac{(x+1)^2(2x-1)^2(4x+1)^2}
{6(4x^2+2x+1)^3},
\qquad x=\cos2\theta.
\]

Its derivative factors as

\[
\mathcal R'(\theta)
=-\frac{18\sin2\theta\,x(x+1)(2x-1)(2x+1)(4x+1)}
{(4x^2+2x+1)^4}.
\]

On `[pi/10,3*pi/20]`, `x>1/2`; hence the derivative is strictly negative. This is an analytic proof that the package cannot be made locally constant by the declared independent affine normalizations and conjugation.

The theorem is about the explicitly defined finite operator envelope, not about a full fusion category.

## 5. Convention and implementation audit

### Concern A — q-Racah sign convention

A raw q-6j matrix depends on trivalent-basis signs. The implementation therefore does not compare entries naively. At the spin-one-half Ising point, fixed diagonal sign gauges transform the direct q-Racah matrix into the P3-A Ising block with residual below `2.2e-81`.

### Concern B — incorrect Racah sum despite orthogonality

Orthogonality alone is not a complete convention proof. The audit adds:

- symmetry of the equal-label matrix;
- the independent Ising gauge bridge;
- high-precision stability;
- direct finite-sum bounds from admissible factorial arguments.

A future independent software implementation remains desirable.

### Concern C — derivative generated by automatic differentiation only

The anchor derivative is separately reconstructed by a five-point finite-difference stencil. The package discrepancy is below `1.7e-29`.

### Concern D — grid overclaim

The quantitative lower bounds are explicitly called grid bounds. Continuum nontriviality is proved by the factorized invariant derivative instead.

### Concern E — spectral result may not test recoupling

The exact invariant uses only the spectrum of `X`. Therefore a recoupling-sensitive auxiliary audit is also recorded: the centered mixed correlation of `X` and `Y` has positive derivative at every sampled point, ranging from approximately `0.8817` to `2.4241`. This is supportive but remains a grid result.

### Concern F — external geometry remains incomplete

The equal-label ray is still only an external control datum. It is not a certified complete classical tetrahedral geometry. This is the main unresolved scientific obligation and blocks transfer to CERT.

## 6. External literature consultation

The direct q-6j construction is consistent with established `U_q(sl_2)` recoupling practice. Taylor and Woodward analyze quantum 6j symbols both at primitive roots of unity and for positive real `q`, relating their semiclassical asymptotics to non-Euclidean tetrahedra. Marché and Paul treat finite-dimensional TQFT curve operators on the punctured torus and four-punctured sphere and identify trace-function principal symbols through Toeplitz analysis.

These works justify the mathematical relevance of q-6j recoupling matrices and finite channel-operator packages. They do not certify the BC nuisance projection, do not identify the present family as a full TQFT away from the root point, and do not supply a physical interpretation of `theta`.

Primary references:

1. Y. U. Taylor and C. T. Woodward, *6j symbols for U_q(sl_2) and non-Euclidean tetrahedra*, arXiv:math/0305113.
2. J. Marché and T. Paul, *Toeplitz operators in TQFT via skein theory*, arXiv:1108.0629.

No advisory Gemini statement is used as evidence.

## 7. Verdict

**Accept P3-B-M2 with restricted claim.**

Assign status:

`THREE_CHANNEL_CONTINUUM_NONTRIVIALITY_AND_GRID_MARGIN_CERTIFIED`.

The P3-B intrinsic direction survives direct q-Racah evaluation, carrier enlargement, a stronger nuisance model, and a compact-interval test.

## 8. Publication firewall

Do not infer from M2:

- deformation of a fixed fusion category;
- continuous standard `SU(2)_k` data;
- a quantum-volume theorem;
- a fully fixed classical geometry;
- observable distinguishability;
- physical dynamics;
- global persistence in label size;
- wall crossing.

## 9. Required next step

Proceed to P3-B-M3: replace the equal-label control ray by a complete externally certified geometric datum and produce an explicit leakage ledger between fixed external geometry and q-dependent internal geometry. CERT remains blocked until that interface is closed.
