# Dual-storage persistence parity

Status: `DUAL_STORAGE_PARITY_PASS`

Canonical GitHub path:
`bc_idpr/p5_to_p2/persistence-gate-v1/`

Canonical Google Drive folder:
`BC-IDPR-P5-P2-Persistence`

Verified objects:

- persistence policy;
- P5 to P2 handoff status;
- U1 certificate summary;
- U2 certificate summary;
- U3 certificate summary;
- U4 certificate summary;
- EQ certificate summary;
- repository/archive index.

Parity fields checked for every family:

- family identity;
- finite level r;
- support rank;
- eta interval;
- uniform derivative upper bound;
- registered cap;
- continuous lower margin;
- normalized lower margin;
- finite-difference diagnostic when present;
- source package SHA-256;
- claim boundary and P2 block.

Large review-only, temporary, or reconstructible intermediates are not required by this gate when downstream work does not read them and the accepted certificate is sufficient.

This parity certificate clears only the persistence gate. It does not complete P5, prove a common-level or arbitrary-level theorem, or authorize P2.
