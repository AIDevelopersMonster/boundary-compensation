# Reproducibility note

The Gateway I demonstrator is deterministic and finite-dimensional.

Run:

```bash
python scripts/reproduce_gateway_i.py
```

The script prints the three threshold-zone crossings and the maximum coarse comparator distance before threshold crossing. Optional figures can be generated with:

```bash
python scripts/reproduce_gateway_i.py --save-figures
```
